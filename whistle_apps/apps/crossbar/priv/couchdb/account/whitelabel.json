{
   "_id": "_design/whitelabel",
   "_rev": "1-5afa51e0f2e8cd806e309876b4bf7d08",
   "language": "javascript",
   "views": {
       "listing_by_id": {
           "map": "function(doc) { if (doc.pvt_type != 'whitelabel' || doc.pvt_deleted) return; emit(doc._id, {'id': doc._id, 'name': doc.name}); }"
       }
   },
   "filters": {
       "export": "function(doc, req) { return ( doc.pvt_type == 'whitelabel' ); }"
   }
}
