{
   "_id": "_design/limits",
   "_rev": "1-50244d7720568cae43532c8a3db1cb45",
   "language": "javascript",
   "views": {
       "limits_by_id": {
           "map": "function(doc) { if (doc.pvt_type != 'limits' || doc.pvt_deleted) return; emit(doc._id, {'id': doc._id, 'name': doc.name}); }"
       }
   },
   "filters": {
       "export": "function(doc, req) { return ( doc.pvt_type == 'limits' ); }"
   }
}
